# Implementation Plans

Generated by the `improve` skill on 2026-08-31, auditing `pipeline/` at commit `3e59f51` on branch
`feat/official-tours-integration`. Execute in the order below unless dependencies say otherwise.
Each executor: read the plan fully before starting, honor its STOP conditions, and update your row
when done.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 005  | Add pytest pixi task + document it | P1 | S | LOW | — | DONE |
| 008  | Argparse help text, stale doc pointer, README indexing | P3 | S | LOW | — | TODO |
| 001  | Dedupe haversine formula into `lib/geo.py` | P1 | S | LOW | — | TODO |
| 004  | Harden zip extraction against zip-slip | P2 | S | LOW | — | TODO |
| 006  | Fix isolated degree-2 cycles dropped by `contract_structural` | P2 | S/M | LOW | — | TODO |
| 003  | Latitude-correct `nearest_point_on_polyline` | P2 | M | LOW-MED | — | TODO |
| 007  | Test coverage for `doit_support.py` + preprocessing/download scripts | P2 | M | LOW | — | TODO |
| 002  | Cache base-graph gather across tour legs in `match_tour_edges.py` | P2 | M | MED | — | TODO |

Suggested order above is by leverage (effort vs. risk vs. how quickly it unblocks confident
follow-on work), not a hard dependency chain — none of these 8 plans technically blocks another.
005 and 008 are listed first only because they're the cheapest, safest wins and 005 gives every
subsequent plan a documented one-command test loop. 002 is listed last because it touches
`match_tour_edges.py`, the file most likely to be mid-edit on the current
`feat/official-tours-integration` branch — land the others first to reduce merge/rebase churn
around it.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- None of these 8 plans has a hard dependency on another. Each plan's own "Drift check" step
  re-verifies its target files against the live code at execution time regardless of order.
- Plan 002 explicitly calls out that `match_tour_edges.py` is under active development (the
  `feat/official-tours-integration` branch is mid-migration toward
  `docs/superpowers/specs/2026-08-30-tour-folder-ingestion-design.md`'s GPX-directory ingestion,
  not yet landed as of this audit) — its executor should re-check the file's current shape before
  starting even if it's picked up soon after 001–008 land in order.

## Findings considered and rejected (from the original 10-item audit table)

These two were surfaced by the `pipeline/` audit but the user chose not to turn them into plans in
this batch — kept here so a future run doesn't re-surface them without reason:

- **#6 — No CI running pipeline tests/lint on push.** Deferred: the user wants to discuss CI and
  linting separately, as a dedicated conversation (tool choice for lint, GitHub Actions workflow
  shape), not as a mechanically-generated plan.
- **#7 — `match_tour_edges.py` undocumented in `phases/README.md`'s DAG diagram and
  `phases/graph_building/README.md`.** Not selected. Worth revisiting once the
  tour-folder-ingestion migration (docs/superpowers/specs/2026-08-30-tour-folder-ingestion-design.md)
  actually lands, since the doc would need updating again immediately after — better to document
  the post-migration shape once, not twice.

Two more findings from the original audit were mentioned as lower-confidence/lower-priority and
were never proposed as plan candidates at all:

- No integrity/checksum verification on downloaded OSM/DEM archives — real but low-severity given
  the offline, non-adversarial trust model (public government/Geofabrik sources); most of these
  providers don't publish stable checksums anyway (Geofabrik/Bayern extracts regenerate daily).
- No dependency-vulnerability scan (`pip-audit`) wired into `pixi.toml` — process gap, not a
  confirmed vulnerable package; folds naturally into the deferred CI/lint discussion (#6) rather
  than standing alone.

## Notes on scope

This batch covers only `pipeline/` (per the original `/improve the pipeline/` invocation) — the
audit did not cover `huts/` (the frontend) or `docs/`. It was standard-effort: hotspot-weighted
coverage across all nine audit categories via 4 parallel subagents, not an exhaustive line-by-line
review of every file.
